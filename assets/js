document.getElementById("themeToggle")
.addEventListener("click", () => {
    document.body.classList.toggle("light-mode");
});

const revenueChart =
    echarts.init(document.getElementById("revenueChart"));

revenueChart.setOption({
    xAxis: {
        type: 'category',
        data: ['Q1', 'Q2', 'Q3', 'Q4']
    },
    yAxis: {
        type: 'value'
    },
    series: [{
        data: [120, 200, 150, 280],
        type: 'line'
    }]
});

const marginChart =
    echarts.init(document.getElementById("marginChart"));

marginChart.setOption({
    xAxis: {
        type: 'category',
        data: ['Q1', 'Q2', 'Q3', 'Q4']
    },
    yAxis: {
        type: 'value'
    },
    series: [{
        data: [52, 58, 61, 64],
        type: 'line'
    }]
});

const forecastChart =
    echarts.init(document.getElementById("forecastChart"));

forecastChart.setOption({
    xAxis: {
        type: 'category',
        data: [
            'Q1',
            'Q2',
            'Q3',
            'Q4',
            'FQ1'
        ]
    },
    yAxis: {
        type: 'value'
    },
    series: [{
        data: [120, 200, 150, 280, 310],
        type: 'line'
    }]
});
